import ProfileEditor from "@/components/ProfileEditor";

export const metadata = { title: "Your profile — Kairos" };

export default function ProfilePage() {
  return <ProfileEditor mode="edit" />;
}
