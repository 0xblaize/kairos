import ProfileEditor from "@/components/ProfileEditor";

export const metadata = { title: "Set up — Kairos" };

export default function OnboardingPage() {
  return <ProfileEditor mode="onboarding" />;
}
